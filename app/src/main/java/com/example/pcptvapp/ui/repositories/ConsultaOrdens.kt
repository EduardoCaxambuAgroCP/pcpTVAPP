package com.example.pcptvapp.ui.repositories

import android.content.Context
import java.lang.reflect.Type

class ConsultaOrdens(private val context: Context) {
    /*
    fun getMotorista(tipo: Int): Motorista? {
        val url: String = "http://192.168.100.209:8082/chamada/consulta"
        val filtro = FiltroChamado()
        filtro.idStatus = tipo
        filtro.idEmpresa = 9
        val json = DoPost.doPost(context, url, Gson().toJson(filtro))
        val type: Type? = object : TypeToken<Motorista?>() {}.type
        val gson = GsonBuilder().setDateFormat("yyyy-MM-dd").create()
        return gson.fromJson(json, type) as Motorista?
    }
     */
}